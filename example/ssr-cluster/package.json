{
  "name": "ssr-cluster",
  "version": "1.0.0",
  "description": "Docker image for pupetteer",
  "main": "ssr.js",
  "scripts": {
    "build": "docker build -t puppeteer-chrome-linux .",
    "start": "node server",
    "docker": "docker run -i --init --rm --cap-add=SYS_ADMIN --name puppeteer-chrome puppeteer-chrome-linux node -e \"`cat server.js`\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^9.0.0"
  }
}
